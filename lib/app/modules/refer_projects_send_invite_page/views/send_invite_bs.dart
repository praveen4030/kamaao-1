import 'package:flutter/material.dart';

class SendInviteBs extends StatelessWidget {
  final String image;
  const SendInviteBs({
    Key? key,
    required this.image,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [],
      ),
    );
  }
}
